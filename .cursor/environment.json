{
  "agentCanUpdateSnapshot": false,
  "install": "bash -lc 'echo PlasmaDX agent install is a no-op on Linux runners'",
  "terminals": [
    {
      "name": "Build Debug",
      "command": "cmake --build build-vs2022 --config Debug"
    },
    {
      "name": "Run Debug (no DXR)",
      "command": "bash -lc 'echo PlasmaDX run skipped on Linux agent'"
    },
    {
      "name": "Run Debug (DXR on)",
      "command": "bash -lc 'echo PlasmaDX run skipped on Linux agent'"
    }
  ]
}